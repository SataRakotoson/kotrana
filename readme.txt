git add -A : ajoute tout les nouveaux modif
git commit -m : ajoute un message après commit

git config --global user.email "you@example.com" : authentifiaction
  git config --global user.name "Your Name" : authentifiaction

git log : lister tout les commit
git push : push vers GitHub
git branch : lister touts les branches
git branch "nom_branche" : créer une branche
git chechout "nom_branche" : changement de branche
